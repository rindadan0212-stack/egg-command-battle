# 履歴

**やったことを残す。** `課題.md` で決着したものが最後にここへ来る。**一方通行の終点。**

⚠️ 直す前にここを引く。⚠️ **「現状維持」も結論。** 比較検討して何もしないと決めたなら、
**その理由を1行残す**（残さないと同じ検討を何度も繰り返す）。

---

## 2026-08-15

- **プロジェクト開始。** 過去作 box の教訓文書2本を読み、転用してよいもの／してはいけないものを
  分類（`参考/` に退避）
- **壁打ちで方向を確定**し `企画.md` を正典として作成。GPT との壁打ち結果を統合し、
  元 txt は `参考/` へ退避（**仕様の出所を1つにするため**）
- **参考作品を実物で調査**（記憶で書かない）:
  - まものダンジョン＋ … 放置のざこ層 + コマンドのボス層という**二層構造**。
    本作は放置を外したので下層は「卵強奪戦」が兼ねる
  - ARK … ステごと独立継承 55% / 変異 7.31% で +2 / **変異カウンタ上限20**。
    出所は公式 Wiki・Dododex・cadon/ARKStatsExtractor
- **段0（観測基盤）完了**（コミット `ccaeedc`）。Vite 8 + TypeScript 7、ポート **5815**

#### 段0 で実測して確認したこと（勘で置いていない）

| 対象 | 実測 |
|---|---|
| `chance(7.31%)` | 100万回で **7.300%** |
| `chance(0.184%)` | 100万回で **0.1896%** |
| `int(0,4)` の一様性 | 24.91 / 24.98 / 24.99 / 25.13 % |
| `sample(4,2)`（両親4枠から2つ抽選） | 各枠 49.9〜50.1%（理論値 50%） |
| 系統の独立 | breeding を1000回消費しても battle の列は不変 |
| パレットスワップ | 殻の画素が `#e8dcc0` / `#b8c8e8` / `#e8c0c8` と定義どおり分岐 |
| 決定論チェックの空振り検査 | 本物の違反2件を捕捉、`determinism-ok` 付きは通過 |
| 照合コマンドの空振り検査 | 閉じたタブ・未コミット16件を実際に検出（嘘の緑を出さない） |

---

## 2026-08-16

- **段A（個体と保管庫）完了。**
  - 未決を2件決着: **各ステ上限 40 / 合計上限 80**（＝1ステ上限の2倍。
    保証したいのは「**得意を2つ作れる**」＝得意2つ×3体で6枠を組む）、**保管枠 50**
  - `stats.ts` を強さの唯一の出所にした（実値・上限・削りを他所に書かない）
  - 種族「タマル」1種 + パレット4色。属性は **牙→羽→鱗→牙** の3すくみ
  - 保管庫画面（並べ替え7種・系譜の下地・1体ずつ実寸で撮れる `#u-<id>`）

#### 段A で実測して確認したこと

| 対象 | 実測（10万回） |
|---|---|
| 合計上限を超えた | 0 |
| 1ステ上限/負値を破った | 0 |
| ⭐ 厳密な順位が入れ替わった | **0**（＝特化が保たれる） |
| 同値の開きが1を超えた | 0（最大1＝整数の端数のみ） |
| 上限以下なのに削られた | 0 |
| 空振り検査 | 上限適用を外すと 85,582/100,000 件が違反 |
| 画面の数値検査 | はみ出し・重なり・文字の見切れ・横スクロール すべて0 |

`hp:40 atk:40 def:10 spd:5 → hp:40 atk:40 def:0 spd:0`。
**高いステは残り、低いステが犠牲になる**が実装で成立している。

#### 気づいたこと（段D/E に効く）

- ⭐ **合計上限があるので「特化」と「合計が低い」は別物になる。**
  強い個体は必ず合計80に張り付き、違いは**配分だけ**。
  → 厳選の目標は「合計を上げる」ではなく「**配分を狙う**」。
  並べ替えの「素質合計」は、育つほど全員80になって役に立たなくなる（各ステ順が主役になる）

#### 決めたこと

- **相対 import に `.ts` 拡張子を付けた。**
  Vite は拡張子なしを解決するが**素の Node は解決しない**。
  段B の自動対戦シミュレータを**バンドラ無しの CLI で回したい**ので、
  ゲームのロジックが `node` で直接 import できる状態を先に作っておく
- **`.live.json` の申告は、中身が変わらなくても毎回送るようにした。**
  「変わったときだけ送る」最適化のせいで、開いているタブが `npm run same` に
  「閉じている」と報告されていた（罠と教訓 §4）。実測で 80秒間 1〜3秒を維持

### 起動と停止（ユーザー要望）

- **`ゲームを開く.bat` を追加。** サーバー起動 → ブラウザ →
  **ブラウザを閉じるとサーバーも終了**する。中身は ASCII のみ・CRLF
  （日本語を書くと cmd.exe が黙って失敗するため）
- **`npm run stop` を追加。** ⚠️ Windows では npm の親を止めても
  vite の子が孤児として残る（罠と教訓 §5）。ポートを握っている PID を落とす
- **判定は SSE 接続が切れることで行う。**

  | 案 | 採否 | 理由 |
  |---|---|---|
  | **SSE 接続が切れたら終了** | **採用** | タブを閉じてもブラウザが落ちても即座に切れる。背面に回っただけでは切れない |
  | 心拍のタイムアウト | 不採用 | ブラウザが背面タブのタイマーを絞る・凍らせるので、**開いているタブを殺す**（§4 で実際に踏んだ形） |
  | `pagehide` の sendBeacon | 不採用 | ブラウザが強制終了したときに飛ばない。SSE なら TCP が切れるので拾える |

- 実測: 画面移動（index→gallery）では**落ちない**（猶予5秒）／
  ブラウザを閉じると**5秒以内に終了**しポート解放／
  `npm run dev` 単体は `autoClose: false` でブラウザ無しでも稼働継続

### 決めたが採らなかったもの（現状維持の記録）

- **DQM 型の配合表**（異種配合で新種族が生まれる）→ **採らない。**
  種族数の二乗で表が膨らむわりに、卵強奪の動機が相対的に弱まる（配合で手に入ってしまうため）。
  種族は親から 50% で継承するだけにする
- **ステルス探索パート**（ARK に忠実な巣への忍び込み）→ **採らない。**
  コマンドバトルとは別ジャンルの実装（検知・視界・逃走）が丸ごと増える。
  戦闘中の二択（倒す / 盗んで逃げる）で同じ緊張を作れる
- **CT を共通時間軸で減らす案**（速度一強への対策）→ **採らない。**
  行動回数で減衰させる方式を採用し、速度一強はステ合計上限で抑える。
  ⚠️ 未実測なので段B で検算する
