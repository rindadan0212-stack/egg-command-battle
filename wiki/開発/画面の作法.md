# 画面の作法

⚠️ **これは実装側の決めごとです。**遊んで発見しえないので攻略ページには書きません。
（旧 Wiki §1 から移設）

### 見た目の正解

> **UI はポップカジュアル、キャラはドット絵。**

⭐ **2層に分けて別々に決める。**混ぜない。

| 層 | 正解 | 出所 |
|---|---|---|
| **UI（画面まわり）** | 太い輪郭 + ぼかし無しの影 + 白と濃紺 | `参考/画面モック.html` |
| **キャラ（モンスター）** | 16×16 のドット絵 | まものダンジョン＋ |

**UI の作法は3つだけ**（`src/style.css` が唯一の出所）:

1. **暗色は `#2B3350` 1色。**輪郭も文字も影も全部これ。他の暗い色を足さない
2. **輪郭（2.5〜4px）+ ぼかし無しの影（`0 4px 0`）。**ぼかした影は使わない
3. **彩度の高い色はキャラだけ。**UI の面は白。塗るのは主導線1つ（オレンジ `#FF9F43`）だけ

**キャラはドット絵のまま。**⭐ **変異＝パレットスワップ**。1体ぶんのドットで変異個体が無限に作れる。
⚠️ モックのモンスターが丸に目と口なのは**モックの手抜き**であって、絵柄の宣言ではない。

### ⚠️ 絵を伸ばすときは縦横比を保つ

⭐ **ドット絵（キャラクター）は、縦横を別々に伸ばさない。**
必ず同じ倍率で拡大縮小する。

⚠️ **判定の幅に絵を合わせようとして潰さない。**
潜入の親を「塞いでいる幅」いっぱいに引き伸ばしていたとき、
親が横に潰れて**別の生き物に見えた**（56〜75 の幅に対し絵の高さは 30）。

⭐ **判定と見た目を合わせたいときは、絵ではなく「面」を判定の寸法にする。**

| | |
|---|---|
| 塞いでいる幅・当たり判定 | **面（帯・板）**で示す。⭐ ここは伸ばしてよい |
| キャラクター | ⭐ **縦横比を保ったまま**、その面の中に収まる大きさで置く |

**伸ばしてよいもの**: 9スライスの器（`panel` / `button` / `pill`）・帯・面・影の楕円。
**伸ばしてはいけないもの**: `PixelSprite` から作った絵すべて（種族・卵）。

⚠️ `Image` なら `preserveAspect = true`、`SpriteRenderer` なら
`localScale` の x と y に**同じ値**を渡す。

---

### スマホの作法（`src/style.css` の器が唯一の出所）

**縦持ち1本。**幅は `min(100%, 430px)`（⚠️ 実機は 320〜430 まである。px 固定にしない）。

| | |
|---|---|
| 器 | **3層**: 固定の上段 / **唯一スクロールする本体** / 固定の下段 |
| 高さ | `100svh`。⚠️ `100dvh` はバーの出入りで値が変わり、固定した下段が動く |
| ページ | `html,body { overflow: hidden; overscroll-behavior: none }` で**絶対にスクロールさせない** |
| 安全域 | `env(safe-area-inset-*)`。⚠️ `viewport-fit=cover` が無いと常に 0 |
| スクロール層 | `overscroll-behavior: contain`（端でページ側へ伝わるのを止める）+ `overflow-x: hidden` |
| タップ域 | **最低 44px 角**。見た目が小さいものは透明な余白で広げる |
| 反応 | `touch-action: manipulation`（ダブルタップ拡大の 300ms 待ちを消す）|
| 手触り | `-webkit-tap-highlight-color: transparent` / `user-select: none` |
| 横持ち | 「粗いポインタ × 低い横長」のときだけ回転を促す（デスクトップには出さない） |

**画面ごとの層分け**（⚠️ 二重スクロールを作らない。1画面に1つ）:

| 画面 | 固定 | スクロール |
|---|---|---|
| ホーム | 全部（収まりきるのが仕様） | なし |
| 巣 | 遊び方の一行 | 巣の一覧 |
| 発射 | 全部 | なし（フィールドを実測で内接させる） |
| 戦闘 | 下のスキルシート | 場（arena） |
| 孵化 | なし | 卵の一覧 |
| 配合 | 上の2枠 + 結果 | 相手の一覧 |
| BOX | 上の詳細 + 並べ替え | グリッド |

⭐ **大きさを決め打ちしない。**発射のフィールドとホームのドット倍率は
`ResizeObserver` で**実際の空き寸法を測って**決める（ドット絵は整数倍だけ選ぶ）。
⚠️ **端末の枠（角丸 + 太枠 + 影）は描かない。**モックのあれは
「7画面を並べて見せるための台紙」であって、設計ではない。

**画面の構成もモックに従う**（色や書体だけの話ではない）:

```
ホーム（ハブ）
  ├ 上段  タイトル / BOX の空き
  ├ GOAL  倒すべき壁の名前
  ├ 舞台  編成3体を三角に配置（リーダーを手前に大きく）+ 台座
  ├ 数値  編成 / スピード合計 / 飛距離
  └ 下段  4パネル（BOX・孵化・配合・探索）── 探索だけ塗る

各画面  上段に ‹ / 見出し / 状態ピル。‹ でホームへ戻る
  巣      巣カード（巣の絵 + 属性バッジ + 名前 + 挑戦）
  発射    フィールドを枠に内接させる
  戦闘    左に味方3体（小さくリスト）/ 右に敵（絵を大きく）/ 下に白いシート
  孵化    卵スロット（殻だけ見せる。中身は孵るまで出さない）
  配合    上に2枠 + RESULT EGG / 下に相手を選ぶリスト
  BOX     上に選んだ1体の詳細 / 下にアイコンのグリッド
```

⭐ **枠1（CTなし）は「その種族の通常攻撃」。**戦闘のシートで幅いっぱいの1行にして、
2・3と質を変える（モックが スキル1 をそう置いていた）。

⚠️ **「必殺の位置」と書いてあったのは誤り**（2026-08-17 訂正）。
枠1 に CT が無いのは**行動できない手番を作らないため**であって、大技だからではない。
⭐ コード側は最初から正しかった（`Skills.cs`「全スキルが CT 中でも必ず打てる札が残る」）。
⚠️ ここを「必殺」と読むと、枠1 に強い技を置く方向へ設計が流れる。**通常攻撃として据える。**

⚠️ **共通ルールの「AIっぽさの回避」（角丸で囲わない / 影を全要素に付けない /
グラデーションを使わない）は、このプロジェクトでは意図的に外している。**
無難に寄せた結果の角丸カードではなく、**1つの明確な選択**をしている。
規則の趣旨（あえて選ぶ）はこちらで満たす。ここを「規則に反している」だけの理由で戻さない。

⚠️ **モックにあって企画に無いものは採らない**（AP / Lv / SP / 実時間の孵化 / 通貨 / WAVE /
パッシブ / 弾数）。⭐ **モックは「見た目と構成」の資料であって「仕様」ではない。**
空いた枠には**実在する事実**を入れる（例: モックの「NOW EVENT」の位置 → 倒すべき壁の名前）。

---

## 個体の札は1つの部品（2026-08-18）

⭐ **BOX の詳細と配合の親札は同じ部品**（`CreaturePanel`）。並びも同じ:

```
┌────────────┬──────────────────┐
│(属)        │ Lv 55/55         │
│            │ タマル           │
│    絵      │ c001 1代 変異0   │
│            ├──────┬────┬────┤
│            │      │素質│強化│
│            │ HP   │  9 │ +6 │
└────────────┴──────┴────┴────┘
┌──────────────────────────────┐
│[特性]                        │
└──────────────────────────────┘
        ┌────────┐
        │  技1   │
        └────────┘
┌────────┐      ┌────────┐
│  技2   │      │  技3   │
└────────┘      └────────┘
```

⚠️ **ラフ図は配置の資料であって、意匠の指定ではない。**
線を引くのは「**無いと読めない**」ところだけ。それ以外は共通ルール
（余白で離す → 面で分ける → 線は一辺だけ）をそのまま使う。

| どこ | 何で分けるか | なぜ |
|---|---|---|
| ステの表 | **横罫**（行ごと）＋ 見出しの下だけ一段濃い1本 | 6行を左から右へ目で追うので線が要る |
| 表の列 | **右揃えだけ**（縦罫を引かない）| 数が揃えば列は立つ。線を足しても読みやすくならない |
| 特性・技 | **面**（明度を一段落とす）| 塊が分かればよい。囲うと表と同じ重さになる |
| 絵・見出し | **余白と字の大きさだけ** | 絵は十分大きく、名前は十分太い。囲う理由が無い |

⚠️ **全部を枠で囲った版を1度作って外した。**絵も見出しも特性も技も同じ重さになり、
「どこを見ればいいのか分からない」札になった。

⚠️ **画面ごとに欄を組み直さない。**組み直した結果、
**特性の働きが BOX にしか出ていなかった**（配合では名前だけだった）。
同じ個体は、どの画面でも同じ顔で出る。

⭐ **違うのは寸法だけ**（`Wide` = BOX の 984 / `Narrow` = 配合の 460）。
⚠️ **狭いからといって積み替えない。**460 でも絵は左、表は右。
積み替えると、同じ個体が画面によって別の札に見える
（実際、一度 縦積みにして「ラフと配置が全然違う」と指摘された）。

⭐ **ステの表は「素質 / 強化」の2列**で、⭐ **足すと実値になる**。

- 素質 ＝ 種族基礎 ＋ 素質（得意・不得意の ±15% 込み）＝ 育てる前の実値
- 強化 ＝ 育てて**実値がいくつ増えたか**（振ったポイントではない）

⚠️ 生の素質（0〜40）だけを出すと、足しても戦闘の数にならず**札から強さが読めない**。
⚠️ 「39（9+6）」と1つの字に詰めるのも駄目 ── 2体を並べたときに桁が揃わず比べられない。

⭐ **見る場所と押す場所を分ける。**押しどころ（出撃・餌・合成・そだてる・技を鍛える・逃がす）は
札の**外**、下に1列。⚠️ 札の中に混ぜると、読んでいる途中で指が触る。

⚠️ **中の枠を器の枠へ食い込ませない。**器（Detail / 親カード）は9スライスの枠を持っていて、
枠の線が内側へ 20px ほど描かれる。⭐ 内側の箱は左右・下に **18px 以上**空ける
（空けないと「箱が枠に切られている」ように見える。実測で外した）。

---

---

## ⭐ 字で説明せず、置き方と動きで分からせる（2026-08-20・作者の指示）

⚠️ 説明の文を画面に置かない。⭐ 読ませるのは**絵と数**だけ。

| 書きたくなること | ⭐ 代わりに置くもの |
|---|---|
| 「のこり 6 回」 | **さいころの絵を6つ並べる。**使ったぶんは輪郭だけに変わる |
| 「壁 ＝ 攻撃」 | **上の帯と盤で同じ絵**（剣）を出す。同じ絵なら結び付けは説明が要らない |
| 「HPが270足りない」 | **錠前の絵**と、押せない札と、暗く落とした道 |
| 「敵×2」 | **髑髏の絵を2つ**置く |
| 「どちらの道を行く？」 | **2つ並べて置くこと**そのもの |
| 「これで最後」 | 最後のさいころを**脈打たせる**（`Throb`）|
| 「なにもない」 | **何も置かない**（無いことは、無いことで伝わる）|

⚠️ **数は字ではない。**`1,258` や `28%` は説明ではなく事実なので置いてよい。
消すのは**言い換え・言い聞かせ・手順の説明**のほう。

### ⚠️ 素材は必ずダウンロードした物を使う

⚠️ 色を塗った四角、`▲`／`▼` の記号、漢字1文字の代用 ── これらは全部
**手で描いた絵**であって、素材ではない。角が立ち、大きさが揃わず、様式が混ざる。

| 通し口 | いつ使うか |
|---|---|
| `Ui.Icon` | 絵の札（`Resources/UI/icon/*`・Kenney CC0）|
| `Ui.Plate` | 9スライスの面（`panel` / `pill`・Hyper Casual UI CC0）|

⚠️ 素の <see cref="UnityEngine.UI.Image"/> に色を塗るのは**線と帯だけ**。
⭐ 器と絵は必ず上の2つを通す。

### ⚠️ 絵の選び方で踏んだこと（2026-08-20）

- `dice`（傾いた2個のさいころ）を残り回数に使ったら、**散らばった破片**に見えた
  → ⭐ `dice_empty`（正面の1個）に。使ったぶんは `d6_outline`（輪郭だけ）
- `direction_n`（方位の矢印）には **`N` の字が入っていた** ── 字を消す方針に反する
  → ⭐ `arrow_right` を 90 度回して上下を作る（`Ui.Icon` の `turn`）

---

## ⭐ ★の枠と、常設のタブバー（2026-08-20・作者の指示）

ソシャゲの意匠に寄せるために足した2つ。

### ★の枠

⭐ 升の**縁の色で序列を出す**（下から 灰→緑→青→紫→金）。
⭐ 上位ほど**縁が太い**（★4以上は 10、それ未満は 6）── 色だけだと序列が読みにくい。

| どこに | 何の★か |
|---|---|
| 卵の升（`Ui.EggCell`）| `Egg.Rarity` |
| 個体の升（`CreatureCell`）| **素質の合計**から逆に引く（`Nests.RarityOfWildTotal`）|

⚠️ **升の「後ろ」には置けない。**升の面は親自身の `Image` なので、子はどう並べても
その上に描かれる。⭐ だから枠は「色の外側 ＋ 白の内側」の2枚組
（`Ui.Ring` と同じ手）。内側を省くと升が塗り潰されて絵が消える。

⚠️ ★5 の金は `Ui.Accent`（主導線の黄）と近い。
⭐ **別の水路**として扱う ── 差し色は押しどころ、★は升の縁。
押しどころに★の色を塗らない限り混ざらない。

### 常設のタブバー

⭐ ホーム / 探索 / 配合 / BOX の4枚。**いま居る所は上辺に差し色の帯**を1本。

⚠️ 札そのものを黄色にしない ── 黄は「1画面に1つの主導線」の色で、
タブに使うと画面の中の主役とぶつかる。⭐ 区切りと同じ約束で**一辺だけ**。

⚠️ **戦闘と潜入では出さない。**タブも「戻る道」なので、‹ を隠しておいて
タブから抜けられては意味がない（`canBack` と同じ条件で出す）。

⚠️ タブのある画面に **‹ を出さない**。タブが行き先を全部持っているので、
‹ は「ホームへ戻る」だけの重複した道になる。

⚠️ タブバーは本体の下 232px を**覆う**（Body は縮まない）。
⭐ 実測で食い込むのは**スクロールする層だけ**だったので、そこだけ窓を縮める
（`App.ReserveDock`）。⚠️ 固定配置の部品には触らない ── 動かすと配置の出所が2つになる。


## ⭐ 体数を変えても壊れない並べ方（2026-08-20・4体化）

⚠️ 立ち位置（`UnitStand` / 放置の歩く器）は **prefab に3つしか置いていない**。
⭐ 体数を変えるたびに prefab を手で触るのをやめるため、
**足りなければ写して増やし、元の占有範囲の中に詰める**形にした
（`BattleView.Fit` / `IdleStrip.Fit`）。

### やり方

1. いま在る器から**間隔を測る**（位置の差）── 縦並びでも横並びでも効く
2. 新しい間隔 ＝ 元の全長 ÷（体数−1）
3. **詰めた比のぶんだけ縮める** ── 縮めないと器どうしが重なる

⭐ Unity で実測した結果（4体・戦闘画面）:

| | 3体 | 4体 |
|---|---:|---:|
| 間隔 | 370 | 246.7 |
| 縮尺 | 1.000 | 0.667 |
| 隙間 | 30 | ⭐ **20**（重なりなし）|
| 下端 | −1280 | ⭐ **−1166.7**（元より内側）|

⚠️ **pivot が左上であることが前提。**別の pivot だと、縮めた瞬間に位置の意味がずれる。

### ⚠️ 横に並べるときの幅の式

⚠️ `(内寸 − 24) / 個数` と書いてはいけない。**隙間の数は「個数−1」**なので、
3個（隙間2つ）でだけ正しく、4個にすると **12px はみ出す**（2026-08-20 に発覚）。

```
1つぶんの幅 ＝ (内寸 − 隙間 × (個数 − 1)) ÷ 個数
```

⭐ 実測で確認（編成画面・4枠）: x = 24 / 261 / 498 / 735、幅 225、右端 **960** ＝ 内寸ちょうど。

## ⭐ 立体を UI に混ぜる（2026-08-20・すごろくのさいころ）

⚠️ 画面（Canvas）は**操作のたびに丸ごと組み直される**ので、さいころは画面の外の層に置いてある。
⚠️ 立体をそのまま Canvas の手前へ置くと、その「組み直しから守る」仕組みの外へ出る。

⭐ **専用カメラで焼いて、`RawImage` に貼る**形にした（`DieCube` → `TrailDice`）。
これなら UI の階層に収まるので、いまの作りを1つも壊さない。

### ⚠️ 踏んだ罠3つ（全部、焼いた絵を見て気づいた）

| 症状 | 原因 | 直し方 |
|---|---|---|
| ⚠️ **4面が重なって見える** | `Sprites/Default` は**両面描画・奥行きを書かない** | ⭐ 専用シェーダ（`Cull Back` ＋ `ZWrite On` ＋ 照らさない）|
| ⚠️ **目が素通しの穴になる** | 元の絵は「丸角の四角に目を抜いた**白いシルエット**」で、塗る前提 | ⭐ **縁から辿れない穴＝目**として取り出し、面を塗り直す |
| ⚠️ **読む面が一番暗い** | 光の向きがカメラの逆 | ⭐ カメラ側（-Z）を明るく |

⚠️ もうひとつ: 傾きの X は**負**。正にすると上面が奥へ倒れて**底面**が見え、
机に転がったさいころの見え方から外れる。

### ⭐ ドット絵に馴染ませる

- テクスチャも RenderTexture も **Point フィルタ**（⚠️ 既定の補間だと回した瞬間に溶ける）
- 回転は**段で送る**（⚠️ なめらかに回すと面がぶれて溶ける）── 目が切り替わる間隔と同じ刻み
- 明るさは**面まるごと1色**（⚠️ 本物のライティングを入れると面の中に階調が出て浮く）
- カメラは本編と同じ **orthographic**（⚠️ パースだけ別だと目に付く）

⚠️ **片づけを忘れない。**RenderTexture は `Release()` ＋ `Destroy()` の両方が要る
（⭐ 実測で漏れ0を確認した）。

## ⚠️ 縮めたら「中心」も合わせ直す（2026-08-20）

⭐ 器を写して増やすとき、**縮めたぶん位置がずれる**。pivot が左上なので、
先頭の位置だけ合わせて縮めると**全体が左（上）へ寄る**。

⚠️ 実測で踏んだ2件:

| 画面 | ずれ |
|---|---|
| 戦闘 | ⚠️ 中心が **437**（画面は 540）── 左に 103px |
| 放置 | ⚠️ 中心が **363**（元は 390）── 左に 27px |

⭐ 直し方は場面で違う:
- **戦闘** … 元の x（60 と 600）が左右で非対称だったので、**幅の 1/4 と 3/4 を中心**に置き直した
- **放置** … 並びは意図して左寄せ（右に敵が居る構図）なので、**元の並びの中心を保つ**

### ⚠️ 「元の場所に詰める」だけでは小さくなりすぎる

⭐ 戦闘の立ち位置は、元の並び（1080）に4体を詰めて **縮尺 0.667** になっていた。
⚠️ だが実際に使える縦は **1290**（技の札の上まで）。
⭐ 使える長さから逆算する形に変えて **0.847** になった（**27% 大きい**）。

```
間隔 ＝ 使える長さ ÷ ((体数 − 1) + 詰まり具合)     詰まり具合 ＝ 器の高さ ÷ 元の間隔
```

⚠️ **隠れている札も数える。**自分の手番でないと技の札は出ないが、場所は変わらない。
`GetComponentsInChildren(false)` にしていた頃、相手の手番に組むと札の下まで並びが伸びた。

## ⚠️ 「本数は決め打ちしない」（2026-08-20）

⚠️ 分かれ道の札を出す所が `for (i = 0; i < ways.Count && i < 2; i++)` と
**2本で止まっていた**。⭐ 盤を 2〜4本にした日に置き去りになり、
**3本目・4本目が画面に出ず、選べなかった**（作者の報告「進めるはずの道が進めない」）。

⭐ **数えるものが増えうるなら、必ず `Count` から引く。**
⚠️ 幅の式も同じ ── `(w - 20) / 2` は2本でだけ正しい。

## ⚠️ 「同じ枠に2つの字を重ねない」

⚠️ 数と単位を**同じ枠**（0,0,幅,132）に置いて、位置だけずらしたら**重なった**。
⭐ 上下の帯を**分けて**置く（数は上76px・単位は下38px）。

## ⭐ 進みは「見せてから動かす」（2026-08-20・作者の指示）

⚠️ 振った瞬間に駒が飛んでいたので、**何マス進んだのか目で追えなかった**。

⭐ いまの流れ:

1. 振る（Core が行き先まで動かす）
2. ⚠️ **駒は前のマスに留めて描く**（`_shownAt`）
3. ⭐ 行き先のマスを光らせ、**そこを押させる**
4. ⭐ 押されたら **1マスずつ**歩く（`TrailWalk`）

⚠️ **歩き終わるまで次の場面へ進めない。**進めると、駒が着く前に雑魚の戦闘が始まる。

## ✅ `画面を検査する` がスクロールの外を「はみ出し」と誤検知していた（2026-08-20 修正）

すごろく画面で **「枠からのはみ出し 14件」**（体数を変える前は 8件）が出ていたが、
⭐ **全部まちがいだった**。

⚠️ 盤（`Board`）は `RectMask2D` を持つ**スクロールの窓**で、
窓の外のマスは**設計どおり隠れている**だけ。「はみ出し」の見方が
親の矩形と比べていたので、スクロールで隠れているものまで拾っていた。

⭐ **直した中身は2つ**:

| | |
|---|---|
| `RectMask2D` を持つ入れ物は**器と見なさない** | 窓は中身より小さいのが当たり前 |
| **切り取られて描かれていない**ものは数えない（`Clip` を通す）| 「重なり」の見方には既に入っていた規則 |

⭐ 直した結果、潜入画面は **重なり0 / 字が枠より広い0 / はみ出し0**。

⚠️ **嘘の警告を出す道具は、そのうち誰にも読まれなくなる。**
⭐ 誤検知を見つけたら、その場で道具のほうを直すこと
（「これは誤検知」と注記して回避すると、次に本物が出ても同じ扱いをしてしまう）。

---

## ⚠️ `画面を検査する` が **絵を1枚も測っていなかった**（2026-08-21 修正）

⭐ 「はみ出し 0 件」は、**字だけを数えた 0** だった。集めていたのは
`Text` と `Button` だけで、絵（`Image`）は1枚も対象に入っていない。

### 足した見方（2つとも曖昧さが無いものだけ）

| 見方 | 何を捕まえるか |
|---|---|
| **画面の外** | 描かれているのに画面の枠の外に居る。⚠️ 巻物の中は数えない（端で切れるのが当たり前）|
| **覆われて見えない** | あとから描かれた・**透けない・絵柄の無い**面に**丸ごと**入っている |

⚠️ **「絵が器からはみ出す」は入れなかった。**器が切り取らないなら、それは崩れではなく
**飾り**。試しに入れて測ったら 42件すべて意図した飾りだった（草の房・いま居る印・
レア度の角バッジ）。⭐ 器のはみ出しは**字だけ**を見る。

### 🔴 見つかった本当の穴 ── **誤差の許容が世界単位のままだった**

⭐ 四隅は**世界の単位**で返ってくる。画面まるごとが縦 10.0 しかないのに、
許容が `0.5f` と生の数で書いてあった ── つまり **96点ぶん**を見逃していた。

```
BOX 画面: 縦に 0.23 離れた別の札に「覆われて見えない」が 3件（全部 嘘）
```

⭐ 直した形 ── **点の大きさを毎回測り直して、そこから許容を出す**:

```csharp
_dot = frame.rect.height > 1f ? (edge[2].y - edge[0].y) / frame.rect.height : 0.005f;
static float Nudge { get { return _dot * 2f; } }   // 2点ぶん
```

⚠️ **検査が甘かったのではなく、目盛りが違っていた。**
⭐ 世界の座標を比べるコードに**生の数を書かない**。必ず画面の大きさから起こす。

### ⭐ 画面を撮る（`Assets/Editor/Shots.cs`）

⚠️ **撮る箇所を口頭で足していかない。**⭐ 一覧は `Shots.Build` の1か所だけ。
画面・札・演出を足したら、**あそこに1行**足す。そうしないと、次に撮ったとき
「何が抜けたか」が分からなくなる。

### 使い方

| したいこと | やること |
|---|---|
| 全部撮る | ▶ を押した状態で **Egg Command / 画面を撮る**（または `Shots.RunAll()`）|
| 中身が寂しい | 先に `Shots.Fill()`（個体14・卵4 を積む。⚠️ **保存を書き換える**）|
| | ⭐ 何度でも回せる。⚠️ 直す前は id が `shot0` 固定で、2回目が「shot0 は既に保管庫にいる」で落ちた（2026-08-21 に直した）|
| 1枚だけ | `Shots.Take("名前")` |
| 置き場所を変える | `Shots.Dir = "..."`（既定はリポジトリの `shots/`）|

⭐ 24枚が **1回の呼び出し**で撮れる。⚠️ 演出は動き出してから撮るので、
`EditorApplication.update` で数フレーム待つ ── 終わると Console に一覧が出る。

### 撮る箇所（2026-08-21 時点・24枚）

| 群 | 中身 |
|---|---|
| **画面**（4）| ホーム / 探索 / 配合 / BOX |
| **潜入**（6）| 振る前 / 行き先が光る / **行き先が遠い**（出目6）/ 歩いている / 関門の払い札 / 払えない関門 |
| **戦闘**（2）| はじめ / 数手すすめた |
| **札**（5）| パーティ編成 / ステータス / 技の詳細 / 分解 / 技の卵 |
| **演出**（7）| さいころ（回っている・止まった）/ ▲の数字とリング / ▼の数字とリング / 衝撃と画面揺れ / 告知の帯 / 生まれた |

⭐ 「行き先が遠い」と「払えない関門」は**壊れていた所を写すため**に入れてある
── 直したあと、同じ絵を撮って比べられる。

### ⚠️ 撮り方で踏んだ罠（3つとも 2026-08-21）

| 罠 | どうなったか |
|---|---|
| `ScreenCapture.CaptureScreenshot` | ⚠️ フレームの終わりに書く作りなので、道具から呼ぶと**直後にファイルが無い**。⭐ 覆いが `ScreenSpaceCamera` なのを使い、カメラを `RenderTexture` へ描いて**その場で**取る |
| 覆いの子を**全部**消した | ⚠️ App 自身の部品（空の絵）まで壊れ、Play を入れ直す羽目に。⭐ 始める前に子の数を覚え、**それより後ろだけ**消す |
| `Screen` が曖昧 | ⚠️ `UnityEngine.Screen` と `View.Screen`（画面の種類）が同名。⭐ 必ず修飾する |

⚠️ **見た目の判定にスクショを使わない。**縮んで届くので被りは分からない
（[上の教訓](#-スクショ検証の落とし穴)）。⭐ 被りと はみ出しは
`画面を全部検査する` の**数**で見る。スクショは「並べて眺める」ためだけ。

---

## ⭐ `画面を全部検査する` を足した

⚠️ この道具は今まで「いま出ている画面」しか見られず、実際には
**触っている画面にしか掛けていなかった**。⭐ そのせいで配合画面の重なり 15件が、
検査を持っているのに見つからないまま残っていた。

```
Egg Command / 画面を全部検査する   ← ホーム・探索・配合・BOX・潜入 を順に回す
```

⚠️ 画面を渡り歩くので、**遊んでいる途中に押すとその潜入は捨てられる**。

---

## ⭐ 手ざわり ── 予備動作 → 動作 → 余韻（2026-08-21）

⭐ 調べた技法は、実装としては**3つの型**に落ちる。どれも数十行で入る。

| 型 | 道具 | どこで使うか |
|---|---|---|
| **潰れて伸びる** | `Squash` | 駒がマスを踏むたび。⚠️ 座標を動かすだけだと「滑って」いる |
| **止める（一拍おく）** | `TrailWalk` の `holdOf` | ⭐ 関門を踏んだとき。**重い所で一拍おくと、そこが重く感じる** |
| **減衰する揺れ** | `Shake` | 敵に当たったとき・見つかったとき |

### ⚠️ 3つとも「必ず元へ戻す」

⭐ `Jolt` `Throb` と同じ決まり ── **座標も寸法も持たない**。
⚠️ 持つと、Prefab で動かした位置や呼ぶ側の配置が効かなくなる。
⚠️ そして `OnDisable` で必ず戻すこと ── 画面は組み直されるので、
潰れた途中で消されると**潰れたまま**の部品が残る。

### ⭐ 揺れの決まり（3つとも実測で決めた）

| | |
|---|---|
| **短く** | `Shake.Life` = 0.28 秒。⚠️ 50〜300ms を外れると「揺れ」に見えない |
| **減衰させる** | 残りの割合を2乗して落とす。⚠️ 一定の振れ幅は機械の振動に見える |
| **層を選ぶ** | ⚠️ 揺らす相手は**組み直されない層**（`App.Overlay`）|

### ⭐ 「数えさせずに、数を見せる」

⚠️ 「出目と進むマス数が合っていない」と疑われたとき、盤を見ても確かめようが無かった
（2026-08-20 の指摘）。⭐ 駒の上に**残りの歩数**を出し、1マスごとに減らした。
⭐ 6→5→4… と減れば、**数えなくても合っていると分かる**。

⚠️ 字で「あと3マス」と書かない ── 数だけ置く。

### ⚠️ 演出は「組み直されない層」へ出す

⭐ 盤は1マスごとに組み直されるので、`Fx`（浮く数字・輪・光）は
`Fx.Get(app.transform)` の層へ出す。⚠️ 盤の中に出すと、次の一歩で一緒に消える。

---

## ⭐ 交換を字なしで見せる（2026-08-21・関門の払い札）

⭐ **左が払う量、右がもらう物、あいだに矢印。**⚠️ 「〇〇を払って△△を得ますか？」と書かない。

⚠️ **押さない選択も同じ大きさで置く。**小さくすると「押すのが正解」に見えて、判断そのものが消える。

### ⚠️ 絵は「既にある組み合わせ」を使い回す

| もらう物 | 絵 | なぜ |
|---|---|---|
| 振れる回数 +N | **さいころ N 個** | ⭐ 上の帯（残り回数）と**まったく同じ文法** |
| その場で N マス | **卵の絵 ＋ +N** | ⭐ 上の帯（卵まで N マス）と同じ組み合わせ |

⚠️ 矢印は使わなかった ── ▲（ステUP）と紛れる。

### ⚠️ 無地の絵は、単独で置くと意味を失う

`icon/die.png` は**無地の丸角四角**（目が無い）。
⭐ 上の帯では何個も並ぶので「さいころ」だと分かるが、
⚠️ 払い札に**1つだけ**置いたら、ただの四角にしか見えなかった（実機で確認）。
⭐ 目のある `die-3` に替えて解決。

**⚠️ 絵は「並びの中で読める」ものと「単独で読める」ものが違う。**流用するときに確かめること。

### ⚠️ 駒は「立っているマス」を隠す

⭐ 駒を 112 → **84px** に縮め、**左下の隅**に寄せた。
⭐ これで**払う量（マスの上の段）は必ず読める**。
⚠️ もらう物（下の段）は隠れるが、そのときは下の札が同じものを出しているのでよい。


---

## ⭐ 敵は左右反転で描く（2026-08-21・作者の指示）

⚠️ 絵は全部おなじ向き（右向き）で描いてあるので、そのまま並べると
**敵も味方も同じほうを向く** ── どちらがどちらか、絵だけでは読めない。
⭐ 敵だけ反転すれば向かい合うので、盤の意味が絵に出る。

| どこ | 何を反転するか |
|---|---|
| 戦闘（`UnitStand`）| 敵側の体だけ |
| 探索の札（`EncounterCard`）| ⚠️ 札に出るのは**その巣の親** ＝ 常に敵 |
| ホームのヌシ（`NestsView`）| 常に敵 |
| 放置の帯（`IdleStrip`）| 相手だけ。⚠️ 味方も**言い切る**（器を使い回すので前の向きが残る）|
| 潜入の盤（`StealStage`）| 親と道中の雑魚。⚠️ 発射台・投げる個体・卵は反転しない |

⭐ **決まりは `Ui.Face(Transform, bool foe)` の1か所。**

| ⚠️ やらないこと | なぜ |
|---|---|
| 札や器ごと反転する | 中の字まで裏返る（HP の数字が鏡文字になる）|
| `x *= -1` と掛け算で書く | 描き直すたびに向きが入れ替わる |
| 「敵かどうか」に既定値を置く | 次に絵を足した人が決めないまま通せる（`StealStage.PixelObject` は既定値なし）|

### ⚠️ **pivot が端だと、反転で絵が枠の外へ飛ぶ**（2026-08-21 に踏んだ）

器の pivot は **(0, 1)（左上）**。そのまま `localScale.x` を負にすると、
**左端で折り返して枠の左外へ出る**。

```
戦闘画面の実測（直す前）
  器（UnitStand） x = 0.31 … 2.98
  絵（Art）       x = 0.31 … −1.35   ← 器の外。味方の列に重なっていた
```

⚠️ **スクショでは「敵が2体並んでいる」ようにしか見えなかった。**
⭐ `GetWorldCorners` で器と絵の左右を突き合わせて初めて分かった
（[上の教訓](#-スクショ検証の落とし穴)の実例）。

⭐ 直し方は「**反転する前に pivot をまんなかへ寄せる**」（`Ui.Face` の中の `Center`）。
⚠️ 寄せたぶんは `anchoredPosition` で埋め合わせる（見た目を動かさない）。
⚠️ 伸び縮みする器（`anchorMin ≠ anchorMax`）は pivot で位置が変わらないので、
埋め合わせない ── 埋め合わせるとそちらが動く。

⭐ 検算はこの1行で足りる:

```csharp
// 絵が器からはみ出していないか（敵・味方の両方で 0 件になること）
aL < pL - 0.01f || aR > pR + 0.01f
```

⭐ **`Mathf.Abs` で書く。**放置の帯の歩き手は元から `scaleX = 0.667` なので、
`±1` を代入すると大きさが変わってしまう（実測で確認）。


---

## ⚠️ 手書き指示から直した6件（2026-08-21）

作者がスクショに赤字で書いた指示。⭐ どれも**数で確かめてから**直した。

### ① 一覧がタブ帯を貫通する

⚠️ 原因は**2つ**あった。

| | |
|---|---|
| 帯に**地が無かった** | ボタン4つだけで、隙間から一覧が透けていた → ⭐ `Dock/Plate` を敷いた |
| 窓が本体をはみ出していた | 実測 **126px**。`ReserveDock` が**直の親の座標**で底を測っていて、窓が入れ子（Body→Stack→Grid）だと途中の親のぶんが抜けていた → ⭐ **世界座標で測る**ように直した |

⭐ `画面を全部検査する`: 配合・BOX の **15件 → 0件**。

### ② パーティ編成

| 指示 | 直し方 |
|---|---|
| ホームでのみ放置／探索でのみ巣 | ⭐ 切り替えタブを**外した**。見出しが「放置の編成」「巣へ潜る編成」を言い切る |
| 並び順がほしい | ⭐ BOX と同じ `SortBar` を一覧の上に置いた |
| 「巣に登録」は不要・Lv優先 | ⭐ 一言は**常に Lv**。もう一方の編成に入っていることは**色**（橙）で示す |

### ③ 潜入

| 指示 | 直し方 |
|---|---|
| 駒の色が同じで見づらい | ⚠️ 駒の円が `Ui.Accent`、**関門のマスも `Ui.Accent`** ── 同じ色だった。⭐ **濃紺の縁＋白い地**に（マスは橙／濃紺／白の3種しか無いので、この2色ならどの上でも浮く）|
| 払い札の数が折れる | ⚠️ 枠 104 に「42,000」（約120要る）。⭐ 180 へ広げ、**折り返しを切った**（桁が伸びても折れない）|

### ④ BOX

| 指示 | 直し方 |
|---|---|
| EXP表示 | ⭐ 見出しの右へ。⚠️ 体数と併記したら **315 要るのに枠 252** で溢れた ── 体数は**下のタブが既に出している**ので外した |
| 長押しで技の詳細 | ⭐ 技の札に `LongPress`。⚠️ **短く触っても開かない**（押しどころではないので誤爆する）|
| レア度は★の数で・色分けは無し | ⚠️ 縁の色（橙・紫・青・緑・灰）をやめ、⭐ **★の数**を絵の上へ。色は覚えないと読めないが、★は数えれば読める |

## ⭐ 絵の通り道（2026-08-23）

🔴 **作った素材が遊ぶ画面に出ていなかった。**web へ写していたのは `icon/` だけで、
`panel` / `button` / 空は CSS で描き直していた ── 素材を足しても web は変わらない状態。

⭐ **`Resources/UI/` の全部を写す**（`icon/` → `wwwroot/icon`、残りは `wwwroot/ui`）。

### 「遊びの概念 → 絵の名前」は `Core.Art` が唯一の出所

⚠️ 絵の名前を画面側に散らかさない。⭐ Core に表を1つ置き、両方のエンジンがそこを読む。

| | |
|---|---|
| `Art.StatusIcon(kind)` | 状態異常 → `icon/status-*` |
| `Art.All()` | ⭐ **表が指す絵、全部**。存在の検査と死蔵の検査はここだけを舐める |
| `Art.Placeholder` | ⭐ **まだ仮の絵**。差し替えたらここから外す |

⭐ **今後キャラクター・ボタン・背景を足すときも同じ形**：
①辞書を1つ増やす ②引く関数を1つ足す ③`All()` に `foreach` を1つ足す。
⚠️ これで検査（実体があるか・誰にも指されていないか）が**自動で新顔も見る** ──
検査を書き直さなくてよい。

⚠️ **描き方（PNG か CSS か）は `Art` では決めない。**CSS で描いている面・押しどころを
PNG へ切り替えるのは別の段（69画面の見た目が一度に変わるので、素材が揃ってから測る）。

### ⭐ 仮の絵を置くときの決まり

⚠️ 「素材は必ずダウンロードした物を使う」の**例外**なので、条件を付ける。

- ⭐ **16×16 で描いて、整数倍で出す**（128×128 ＝ 8倍）。半端に拡大するとドットが不揃いになる
- ⭐ **白の抜き＋色を掛ける** ── 既存の絵と同じ約束（良い側は緑・悪い側は赤）
- ⭐ **作り直す道具を残す**（`scripts/gen-status-icons.mjs`）。`#`／`.` の格子で描いてある
- ⭐ **`NOTICE.md` に「自作の仮」として出所を書く**。⚠️ 借り物と混ぜない
- ⭐ **`Art.Placeholder` に載せる** ── 残り枚数が検査から数えられる

### ⭐ 種族の絵は「あらかじめ差し替えた PNG」で通す（2026-08-23）

⚠️ **上の `Core.Art` とは別の通り道。**`Art` は「名前（文字列）→絵」の表で、
骨組みの `pic=` が名前をそのまま指す。種族の絵は骨組みが**種族もパレット添字も知らない**
（`fill.Sprite`/`fill.Palette` が返す `PixelSprite`/`Palette` の**実体**しか渡ってこない）
ので、名前の表では引けない ── だから `SpriteManifest`（Web）が**オブジェクトの参照**で引く。

| 段 | 唯一の出所 |
|---|---|
| 焼く | `SpritePng.RunDisplay`（Sim）── `sim sprites` で (種族×パレット) の全通りを `art/sprites/display/<種族id>-<パレット番号>.png` へ |
| 配る | `EggCommand.Web.csproj` の `CopyArt`（`art/sprites/display/*.png` → `wwwroot/sprite`）+ `SpriteManifestFiles`（実在一覧を埋め込み） |
| 引く | `SpriteManifest.StemOf(sprite, palette)` ── `species.Palettes[i]` は種族ごとに1個しか無いので、参照の一致で `"<id>-<i>"` に変わる |
| 出す | `LayoutDom.Dots` が `stem` があれば `<img src="sprite/<stem>.png">` を1枚、無ければ（卵など）今までどおり SVG |

⚠️ **なぜ実行時に色を合成しないことにしたか**: 変異＝パレットスワップだが、
組み合わせは (種族×パレット) で**高々38通り**の有限集合。⭐ 有限で小さいなら、
毎回合成するより**先に全部焼いておくほうが速い**（1画素＝1 `<rect>` だった頃、
BOX 1画面で矩形32,535個・DOM 32,663個。詳細と実測は `課題.md`「絵1枚が矩形1,085個」）。

🔴 **焼き忘れると「古い絵が出る」を黙って許さない**（この作品が一番嫌う「出所が2つに割れる」形）:
- **テスト側**: `SpritePngTests.表示用PNGが最新である` が全38通りをその場で焼き直し、
  `art/sprites/display/` の実物とバイト単位で比較する。ずれれば「sim sprites を走らせてください」
- **画面側**: `SpriteManifest.Exists` が実在しない絵を `icon-missing`（🔴 黙って空の四角にしない）で出す

⚠️ **卵（`EggArt`）はまだ焼いていない**（種族表の外・View 側 `StealStage.cs` に絵が埋まっている）。
`SpriteManifest.StemOf` が種族表に見つけられない絵は、今までどおり SVG の `<rect>` で描く。

### 🔴 色は「掛けた側」ではなく「持っている側」の得失

⚠️ 挑発を**良い側（緑）**で出していた（2026-08-23 修正）。挑発は相手に付ける弱化なので、
緑だと「相手が強くなった」と読めてしまう ── **狙い先を選ぶ、まさにその瞬間**に逆の意味を出す。

⭐ 根っこは「`ActiveStatusBadges` に検査が1件も無かった」こと。
⭐ いまは 15 件（強化7・弱化8）が好悪を全種類ぶん押さえている（`StatusBadgeTests`）。

---

### ⚠️ 撮影の道具も直した

⭐ `Shots.Fill()` は**前に積んだぶんを先に捨てる**。⚠️ 捨てないと4回目で
「保管庫が満杯（50枠）」で落ちた。⚠️ 捨てるのは `shot〜` だけ（手で作った個体には触らない）。
