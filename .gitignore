node_modules/
dist/

# 🔴 **参考資料。版管理に入れない**（2026-08-28）。
# `まもダン_*` は mamodan-library.com から取った 584体ぶんのキャラデータで、
# ⚠️ ファイル自身が「著作権は株式会社clony」と名乗っている。手元で読む資料であって、
# 配るものではない ── 公開リポジトリに入れると商用ゲームのDBの再配布になる。
# ⭐ 消していない（ディスクには在る）。`sim mamo` は無ければ「参考データが無い」と言って戻る。
参考/

# 実行中の状態の観測用。git に入れない（§5.0-②）
.live.json

# Playwright MCP の作業出力（スクショ・ログ）
.playwright-mcp/

*.local
Thumbs.db

# 撮った画面（`npm run shot` が書く）
shots/
records/shots/

game/**/bin/
game/**/obj/

# pixelizer/ はドット絵エディター。ここに置いてあるが、自前の git リポジトリを持つ
# 別の作品（https://github.com/rindadan0212-stack/pixelizer）。
# 外さないと、この repo が入れ子の repo として飲み込んでしまう。
pixelizer/

図鑑.html

# 帳面の編集画面。sim sheet html が作り直すので版管理に入れない（図鑑.html と同じ扱い）。
# ⚠️ sheets/*.txt のほうは手で書いた書きかけとメモが入るので**入れる**。
sheets/エディタ.html

# ⚠️ 素材の写し。⭐ 毎回のビルド（`CopyArt`）が作り直す。
# 🔴 **唯一の出所は `assets/` と `art/sprites/display/`。**
#    ここを版管理に入れると出所が2つに割れる（2026-08-25 に ui/ と sprite/ が
#    実際に載っていたので外した）。
game/EggCommand.Web/wwwroot/fonts/
game/EggCommand.Web/wwwroot/icon/
game/EggCommand.Web/wwwroot/paint/
game/EggCommand.Web/wwwroot/ui/
game/EggCommand.Web/wwwroot/sprite/
