# 孵化器の1枠 ── ⭐ **部品**（器は 300x380）
#
# ⚠️ 枠の絵（`paint`）は差し込む側（`home.txt` の `slot` 行・`pic=slot-frame`）が持つ
#    ── slot.txt 自身には枠の絵は無い（唯一の出所が二重にならないように）。
# ⭐ 2026-08-25 ドット絵化 段取り4: `art` を 168×168 → **256×256**（64ドット×4）にした。
#    ⚠️ 「絵は拡大縮小しない」規則により、種族の絵（64×64ドット）は必ずこの大きさで置く
#    （計画 §2/§5）。⚠️ 枠（300×380）に収めるため、下の字4つの高さを詰め直した
#    （字は枠より高く描いても切れない ── `stage.css` の `.n.label` に overflow:hidden は無い）。
#
#  名前   種類    左   上    幅   高    付け足し
empty    label   0    168   300  44    size=40 ink=faint anchor=center when=!full text=からっぽ
ready    round   44   16    212  212   when=ready bind=ready
art      pixel   20   4     256  256   when=full bind=art
stars    label   0    260   300  28    size=40 ink=accent anchor=upper-center when=full bind=stars
track    bar     32   292   240  16    when=full bind=track
clock    label   0    312   300  32    size=40 anchor=upper-center when=full bind=clock
# ⭐ **何を温めているか。**⚠️ ★と時計は「どれくらい」しか言わないので、
#    これが無いと**何が孵るのか**を言う字が画面に1つも無い（2026-08-22）。
who      label   0    348   300  28    size=40 ink=dim anchor=center when=full bind=who
