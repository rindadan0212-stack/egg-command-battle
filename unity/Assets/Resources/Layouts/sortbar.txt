# 絞り込みと並べ替えの、畳んだ1行 ── ⭐ **部品**（`use=sortbar` で差す）
#
# ⚠️ この骨組みは単体では画面にならない。BOX・配合・編成が差して使う。
# ⭐ 差す側が位置を決める（この中は 0,0 からの相対・器は 984x112）。
#
# ⚠️ **開いた中身はここに無い**（`sortchips.txt`）。
#    ⭐ 開いたときに使える高さは画面ごとに違うので、**巻物の高さは差す側が決める**。
#    ここに書くと、いちばん狭い画面（BOX）に合わせて全部が縮む。
#
# ⭐ **行そのものが押しどころ。**⚠️ 中に釦を置かない（どこを押すのか読めなくなる）。
# ⭐ ここは状態を出す1行で、主役ではない ── だから塗らない
#    （塗ると画面で一番目立ち、アクセントが効かなくなる）。
#
#  名前     種類    左   上   幅   高    付け足し
plate      card    0    0    984  112   tap=toggle
now        label   28   0    872  112   size=28 anchor=left bind=now
arrow      label   908  0    48   112   size=30 ink=dim anchor=right bind=arrow
