# 手に入れた瞬間・生まれた瞬間の全画面演出 ── Unity 版 Fanfare.prefab の実測をそのまま写す
#
# ⚠️ **覆いは画面いっぱい。**⭐ Banner と違って後ろを完全に止める（覆いの後ろは押せない）。
#    ⚠️ **覆い自体が押しどころ**（`tap=cheer`）── Unity も `Dim` が Image と Button を
#    兼ねていて、画面のどこを押しても閉じる（close 専用の小さな釦は無い）。
#
# ⚠️ **光（burst）は絵（art）と別の器（別の根）に置く**── Unity は Burst を Pop の子にして
#    弾む動きへ巻き込んでいるが、web は回す（`transform:rotate`）ぶん `getBoundingClientRect`
#    の外接矩形が段によって膨らむ（正方形を回すと対角が四角の外へ出る ── 見た目は円なので
#    絵は1px も動いていないのに、判定だけが「親からはみ出した」と読む）。⭐ さいころの
#    `face`（`dice.txt`）と同じ理由で根に置く ── 根の子は「親からのはみ出し」を見ない
#    （`scripts/audit.mjs` の③）。位置は pop の中に居たときと同じ絶対座標のまま。
#    ⚠️ 代わりに失うのは「光も pop と一緒に弾む」だけ ── 円は回しても見た目が変わらないので
#    実害はほぼ無い（弾みは pop 側の art/stars だけで十分伝わる）。
#
# ⚠️ ★（stars）は**卵のときだけ**出す。誕生では `Cheer.Born` が空文字を渡すので
#    `when=stars` が丸ごと隠す（Unity は `gameObject.SetActive` で同じことをしている）。
#
# ⚠️ 動きの秒数はここに書かない。`stage.css` の `@keyframes cheer-pop`/`cheer-spin` が
#    `Core.Beats.CheerPop`/`CheerSpin` と同じ数を持つ（唯一の出所は Beats）。
# ⚠️ 字の色は `stage.css` の `#stars-fan`/`#line-fan` が持つ（濃い覆いの上で読める色に
#    差し替えた ── Unity の実測色そのままだと web の濃い覆いの上では沈んだ・実測 1.1〜1.8:1）。
#
#  名前   種類    左   上    幅   高    付け足し
dim      veil    0    0     1080 1920  tap=cheer
burst    round   140  420   800  800   bind=burst
pop      box     140  420   800  800
  # 🔴 **ここだけは大きく見せてよい**（2026-08-29・作者の指示「引き伸ばしていいのは
  #    ほかの比較対象がないとき（孵化後の演出の時とか）」）── 画面には生まれた1体しか
  #    居ないので、隣と見比べて「大きい種族だ」と誤解される心配が無い。
  # ⭐ 512 は 64x8 ── 64ドットの種族は 8px/ドット、16ドットの種族は 32px/ドット。
  #    ⚠️ 以前は 480 の枠に 256px（64ドット）／64px（16ドット）しか出ていなかった。
  # ⭐ **絵の下の影**（2026-08-30・作者の指示「キャラクターが映っている画面ではすべて同様に」）。
  #    ⚠️ 絵より前に書く（下に敷くため）。⭐ 裾（600）に 40 食い込ませてある。
  #    ⚠️ 濃さは `Face.ShadowCss` の1つだけ ── `unit.txt` の影と同じ物。
  # ⚠️ 種類は `bar`（`round` は正方形で描かれるので平たい影にならない ── `unit.txt` 参照）。
  shade  bar     280  560   240  60    bind=shade
  art    pixel   144  88    512  512   bind=art
  stars  label   0    620   800  80    size=56 anchor=center bind=stars when=stars
line     label   40   1330  1000 200   size=54 anchor=upper-center wrap=yes bind=line

# 🔴 **生まれたその場の「分解」「くわしく見る」**（2026-08-29・作者の指示）。
# ⚠️ **卵を得たとき（`Cheer.EggGot`）には出さない** ── 分解も詳細も
#    生まれた個体にしか意味が無い（`when=creature` が `Cheer.IsCreature` を見る）。
# 🔴 **`dim` より後ろに書くだけでは足りない。**⚠️ `dim` は `tap=cheer` を持つ
#    ── 押しどころどうしの重なり検査（`Layouts.Faults`）は「地」を持たない
#    釦（`tap=`/`hold=` の無い箱）とだけは見比べない。だから直下の兄弟に置かず、
#    ここで押しどころを持たない箱（`choice`）に一段くるむ ── `dim` と比べられるのは
#    その箱自身（押しどころを持たない）だけになり、実際の釦（`fuse`/`detail`）は
#    箱の中どうしでしか比べられない。⭐ 押しどころが覆いに食われないことは、
#    それとは別に「あとに書いた節点が上に乗る」（`LayoutDom`）ので満たされている。
choice   box     140  1560  800  112
  # ⭐ 分解＝`tap=fuse` と**同じ流れ**（BOX の「分解」釦と同じ口）。
  #    ⚠️ 開く前に祝いを閉じ、生まれた個体を選ぶのは `Shell.Tap` の
  #    `case "fuse"` 側（`ClaimBorn()`）が行う ── ここでは骨組みは判断しない。
  fuse   button  0    0     380  112   size=30 tap=fuse when=creature text=分解する
  # ⭐ 詳細＝`tap=detail`（BOX の詳細札へ着地 ── 2026-08-29 付け替え）。
  #    🔴 grow は6本全部が**不可逆の EXP 消費釦**の札 ──「くわしく見る」の着地として誤り。
  #    ⚠️ 新しい画面は作らない ── Sheet.Box ＋ 本人選択（ClaimBorn）で、BOX の升を
  #    押したのと同じ姿（詳細札＋一覧）になる。
  detail button  420  0     380  112   size=30 tap=detail when=creature text=くわしく見る

# 🔴 **その場で重ねて見せる詳細**（2026-08-30・作者の指示「BOXに飛ぶのではなく
#    BOXで表示する詳細と同じものを見れるだけに」）。
# ⭐ **中身は BOX とまったく同じ部品**（`use=panel`）── 画面ごとに組み立てを書くと、
#    同じ個体が場所によって違う顔になる（`panel.txt` の冒頭に書いてある通り）。
# ⚠️ `panel.txt` には押しどころが1つも無い（強化も分解も `box.txt` 側の釦）ので、
#    差し込むだけで「読むだけ」になる ── 見せないための細工は要らない。
# ⚠️ **`choice`（分解／くわしく見る）は同時に出さない** ── 出すと詳細の上に釦が
#    重なる。`Sheets.Fanfare` の `when=creature` が `!BornLook` を見て消す。
# ⚠️ 閉じる釦は**押しどころを持たない箱の中**に置く（`choice` と同じ理由 ──
#    `dim` が `tap=cheer` を持つので、直下の兄弟に釦を置くと重なり検査に掛かる）。
look     box     0    0     1080 1920  when=look
  sheetp card    48   340   984  760   use=panel
  lookx  button  280  1160  520  112   size=34 tap=detail text=閉じる
