# 戦闘 ── ⭐ 編成ぶん同時・スピードゲージ制・技ごとの CT
#
# ⚠️ **下の帯を出さない画面**（戻れると、不利な盤面をいつでも無かったことにできる）。
#    ⭐ だから本体は 1788 まで使える ── `dock=no` はその宣言。
#
# ⭐ **立ち位置は `host`。**⚠️ 体数から間隔と大きさを逆算するので、座標を書き出せない
#    （`BattleView.Lay` の式）。骨組みが持つのは**枠だけ**。
#
# ⚠️ 「決着」と技の札は**同時に出ない**ので重ねてよい（`when=done` で入れ替わる）。
#
#  名前    種類    左   上    幅   高    付け足し
# ⭐ **入切の札なので、いまどちらかを字で出す**（「オート  ON」／「オート  OFF」）。
# ⚠️ **180 では入らない。**「オート  OFF」は 32px で 194 要る（実測 2026-08-22）。
#    ⚠️ 入らなかった頃は**黙って2行に折れて**いた ── 釦は既定で折り返すので、
#    「はみ出し」としても「高すぎ」としても検査に映らなかった。
# 🔴 **小さく・右の端へ寄せる**（2026-08-30・作者の指示「小さなボタンで表示。
#    押す頻度の低いものなので」）。⚠️ **高さは 112 のまま** ── これは意匠でなく
#    指で押せる下限（`Layouts.TapHeight`）で、`TapTooShort` が見張っている。
#    ⭐ 小さくできるのは**幅と字**（300→200・32→24）。
# ⚠️ 24px なら「オート  OFF」は実測 194x24/32 ≒ 146 で 200 に収まる
#    （32px で 194 要る、という 2026-08-22 の実測から比で出した）。
pick      button  840  16    200  112   size=24 tap=pick bind=pick

# ⭐ **あきらめるは負けと同じ扱い。**⚠️ 只で抜けられると、
#    不利な戦いをいつでも無かったことにできてしまう。
# ⚠️ 取り返しがつかないので、`ask.txt` で一度だけ確かめる。
# ⚠️ **`text=` は行末まで飲む。**⭐ 後ろに付け足しを書かない（`when=` は前へ）。
give      button  620  16    200  112   size=24 tap=give when=!done text=あきらめる

# 🔴 **丈を 1278→1440・上端を 150→140 へ**（2026-08-30・作者の指示「1体のサイズを
#    もっと大きく」＝**戦闘に出ているキャラ全員**を大きく、の意）。
# ⭐ `Stands.Lay` は「使える高さから、詰まらない最大の大きさを逆算する」ので、
#    枠を広げると体が大きくなる。⚠️ ただし**広げるだけでは半端な縮め率になる**
#    ── `Stands.Step` を 380→360 に詰めて、4体のとき縮め率がちょうど **1** に届くようにした
#    （1440 ÷ (3 + 350/360) = 362.5 → 歩幅は 360 で頭打ち → 360/360 = 1）。
# 🔴 **縮め率 1 でないと絵が濁る。**⚠️ `transform: scale()` は絵にも掛かるので、
#    0.933 のままだと `FitArtStyle` が「64升あたり3画素」で置いた 192px の絵が
#    **179px（2.8 画素/升）** で描かれていた（実測）。1 なら 192px＝3画素/升の整数。
# ⚠️ **これ以上は大きくできない。**⭐ 絵を次の段（256px＝64x4）にするには器も 64 伸びて
#    `High` 414・`Step` 424 になり、縮め率 1 に要る丈は `3×424 + 414 = 1686`。
#    使える丈は最大でも `技の札の上端 1628 − 上端 136 = 1492` で **194 足りない**
#    （4体を縦に並べる限り、どう詰めても届かない ── 報告済み）。
# ⚠️ 状態欄の「+N」（器の 384..412）だけは、いちばん下の体で技の札に 24 潜る。
#    ⭐ 絵の並び自体（298..362）は 1582 で収まっており、潜るのは5個以上ついたときの
#    余り表示だけ。札があとに描かれるので隠れる（重なって読めなくなる字は出ない）。
# 🔴 **この数は `Sheets.Fight` が骨組みから読む**（2026-08-30）── 前は
#    `Column(s, allies, 540, 1278, …)` と**写しが埋め込んであった**ので、
#    ここを直しても実物は動かなかった。
allies    host    0    140   540  1440
foes      host    540  140   540  1440

# ⭐ 1枚目だけ広い（上段1枚・下段2枚）
# ⚠️ **3枚をまとめて `when=!done` の中に入れる。**⭐ 「決着」と入れ替わることを
#    1つの名前で言わないと、検査は**同時に出る2つが重なっている**としか読めない。
# 🔴 **画面の下端につける**（2026-08-30・作者の指示「技ボタンエリアを下につける」）。
# ⚠️ 前は 1440..1788 で、下に 132 の余りが浮いていた ── 上のバーを外して丈が
#    1788→1920 に伸びた（2026-08-29）のに、この札だけ元の位置に残っていたため。
# ⭐ 1608 + 312 = 1920 でぴたり下端。中身は 8..284 なので、画面の下に 28 だけ残る。
hand      box     0    1608  1080 312   when=!done dock=no
  # ⭐ 長押しで技の詳細（skillinfo）── BOX の札（panel.txt）と同じ道（2026-08-29）。
  #    ⚠️ 短押し（tap=sN・技を出す）とは tap.js が押下時間で分ける ── 触っただけでは開かない。
  # ⚠️ **幅を 650→620 に、左を 215→48 へ**（2026-08-30）── 右に「特性」の釦を置いたため。
  s0      card    48   8     620  130   when=s0 bind=s0 tap=s0 hold=s0
    s0name  label   8    16    604  44    size=34 anchor=upper-center bind=s0name
    s0pill  card    235  70    150  40    bind=s0pill
      s0ct  label   0    0     150  40    size=22 anchor=center bind=s0ct
    s0lv    label   12   74    120  40    size=24 anchor=left bind=s0lv
  # ⭐ **特性を読む釦**（2026-08-30・作者の指示「特性を確認できるボタン追加」）。
  #    ⚠️ 戦闘中は BOX の札を開けないので、いま手番の体の特性を確かめる道が
  #    どこにも無かった（特性は勝ち負けを決めるのに、盤には名前すら出ていない）。
  #    ⭐ 出し方は `Say`（一言）── 覆いを開くと時計が止まり、戦いの流れが切れる。
  feat    button  708  8     324  130   size=30 tap=feat text=特性
  s1      card    48   154   480  130   when=s1 bind=s1 tap=s1 hold=s1
    s1name  label   8    16    464  44    size=28 anchor=upper-center bind=s1name
    s1pill  card    165  70    150  40    bind=s1pill
      s1ct  label   0    0     150  40    size=22 anchor=center bind=s1ct
    s1lv    label   12   74    120  40    size=24 anchor=left bind=s1lv
  s2      card    552  154   480  130   when=s2 bind=s2 tap=s2 hold=s2
    s2name  label   8    16    464  44    size=28 anchor=upper-center bind=s2name
    s2pill  card    165  70    150  40    bind=s2pill
      s2ct  label   0    0     150  40    size=22 anchor=center bind=s2ct
    s2lv    label   12   74    120  40    size=24 anchor=left bind=s2lv

# ⚠️ **「決着」の釦は置かない**（2026-08-23 に外した）。⭐ 告知（`banner.txt`）が
#    出て、読ませて、自分で消えて、次へ渡す ── Unity 版と同じ形。
#    ⚠️ 釦にすると「押したから終わった」に見えるし、
#    告知の 1.17秒 だけ出て勝手に消える釦になっていた。
