# ホーム ── ⭐ 上半分が放置、下半分が孵化器
#
# ⚠️ 孵化は独立した画面をやめてここへ移した。空いている枠を押すと、
#    そのとき初めて卵の在庫が開く（棚を常に出しておかない）。
#
# ⭐ **放置の帯は `host`。**⚠️ 歩く3体と敵の位置は時間から逆算していて、
#    座標を書き出せない ── 「まだ移していない」のではなく「骨組みが持たない」と宣言する。
#
# ⚠️ 数は `Resources/Prefabs/HomeScreen.prefab` の実測（枠は 3列×2段・倍率1）。
#
# ⭐ 2026-08-25 ドット絵化 段取り4: `mats`/`slot`/`trial`/`party`/`keep` は絵（`paint`）を敷いた。
#    ⚠️ いまは仮置き（`sim paint-placeholder`）── 枠線＋薄い塗り＋隅の印で、本物と見間違えない。
#
#  名前   種類    左   上    幅    高    付け足し
# ⭐ **背景（作者のドット絵）。**5層で、奥から 空 → 山 → 遠くの地面 → 草木 → 地面。
#    ⚠️ **一番上に書く ＝ 一番奥に描かれる**（あとの節点が上に乗る）。
#    ⭐ 2026-08-29: 空を作者の描き直しに差し替え、**遠くの地面**を足した。
#
# ⭐ **奥の3層は流れる。**⚠️ 絵は「元・鏡・元・鏡」の**4枚**を横に並べてある。
#    ⭐ **2枚ぶん**流したら先頭へ戻す ── 模様の周期が「元＋鏡」＝2枚ぶんなので、
#    流し終わりの見え方が流し始めと1画素も違わない（実測で確認済み）。
#    🔴 **2枚幅で1枚ぶん流す作りでは駄目**（2026-08-29 まではそうだった）── 流し終わりに
#    映っているのは「鏡」なので、先頭へ戻る瞬間に**絵が左右反転してパッと切り替わる**。
#    ⚠️ 絵を作るのは `tools/bg-band.mjs`（唯一の出所は `assets/ui/home-src/`）。
#    ⚠️ 動きは級（`roll=sky` / `roll=hill` / `roll=far`）で掛ける ── 骨組みは
#    **置き場所だけ**を持ち、速さは `stage.css` が持つ。
#    ⭐ **手前ほど速い**（空12 → 山24 → 遠くの地面48 px/秒。作者の指示 2026-08-29）。
#    🔴 **id で動かさない。**別の画面に同じ名前があると両方に効く
#    （2026-08-26 に `#ground` でそれを踏んで、すごろくが遊べなくなった）。
#
# ⚠️ 上端は作者の絵の実測（山=102 / 草木=55 / 地面=77 ドット）。×4 で px。
#    ⭐ 遠くの地面の 420 は「下端が 680」から逆算 ── 草地の帯が `grass`（588 から
#    全幅が埋まる）の裏に隠れる位置。
#    ⚠️ 2026-08-29: 遠くの地面を作者の2枚目に差し替え。**実測がまったく別**
#    （1ドット=8px・すでに横向き・地は最初から透明）── `tools/bg-band.mjs` 参照。
skyband  paint   0    0     4320 576   pic=home-sky roll=sky
hillband paint   0    408   4320 252   pic=home-hill roll=hill
farband  paint   0    420   2560 260   pic=home-far roll=far
# ⚠️ 地面は**下の帯の裏まで敷く**背景なので、潜っていてよい（`dock=no`）。
ground   paint   0    228   1080 1612  pic=home-ground dock=no

# 🔴 **上の帯の絵（`home-mats`）を外した**（2026-08-30・作者の指示「帯を消して上に詰める
#    背景に被せるように」）。⭐ `box` は**何も描かない入れ物** ── 中身（EXP の丸と数）の
#    置き場所だけを保ったまま、地の板だけを消せる。
#    ⚠️ 絵そのもの（`assets/ui/paint/home-mats.png`）は消していない（`idleframe`・`plate` と
#    同じ扱い ── 戻したくなるかもしれない）。
# ⚠️ これで EXP は**空の絵の上に直に**乗る。字は既定の濃い色なので、明るい空の上で読める
#    （実測で確認）。
mats     box     0    0     1080  80
  icon   round   48   20    40    40    bind=icon
  # ⭐ **EXP と書く。**⚠️ 数だけ出していた頃は、丸い印の隣の数が
  #    何の数なのか画面のどこにも書いていなかった。
  count  label   104  0     400   80    size=40 anchor=left bind=count
  # ⚠️ 世界番号（旧 world ── ここに居た）は保存の控えの札（save.txt）の中へ移した
  #    （2026-08-29 ── 右端を「控え」の入口に譲るため）。

# ⭐ **輪の外のものをまとめた入口**（2026-08-29・作者の指示「図鑑や保存などのボタンにしか
#    使わないものを一か所にまとめ、ホーム画面の帯を消した後の右上に」「右上は入口1つにして、
#    押すと4つが開く」）。⚠️ 中身は `menu.txt`（図鑑・試練・パーティ編成・保存の控え）。
# 🔴 **これ以前は3か所に散っていた**: 図鑑＝上のバーの右肩・控え＝この帯の右端・
#    試練と編成＝巣の菱形の真ん中。上のバーを外したのを機に1つに寄せた。
# ⚠️ 押しどころの下限 112 は mats（高80）に入らないので、**根に置いて帯から 32px
#    はみ出させる**（mats の子に置くと「親の枠から縦へはみ出し」の不備になる。
#    根どうしなら paint × button の重なりは不備でない）。
#    見た目は ink=clear で字だけ ── 上の帯の絵を隠さないため。
menu     button  832  0     200   112   size=32 tap=menu ink=clear text=そのほか

idle     host    0    196   1080  472
# 🔴 **放置帯を囲む手書きの枠は非表示にした**（作者の指示 2026-08-28）。
#    ⚠️ 絵そのもの（`assets/ui/paint/home-idle-frame.png`）は消していない ── 戻したくなる
#    かもしれない。⭐ 消したのは節点だけ（`idleframe paint 16 108 1048 460
#    pic=home-idle-frame` という1行だった）。

# ⭐ **4つ目の `host`。**⚠️ 巣5つは 2-1-2 の菱形に置いてあり（作者のドット絵
#    「ホーム画面」の実測）、格子ではないので `repeat=` の桁と段では書けない
#    ── 置き場所は `Incubator.cs` が持つ（`Board` のマスと同じ理由）。
# ⚠️ 中の1つぶんは `slot.txt`（304x376）。ここは枠だけ。
nests    host    0    716   1080  880

# 🔴 **試練と編成の釦をここから外した**（2026-08-29・作者の指示「輪の外のものを
#    一か所にまとめて右上に」）。⭐ 行き先は右上の `menu`（`menu.txt`）。
#    ⚠️ 敷いていた絵（`home-trial` / `home-party`）は消していない ── 戻したくなる
#    かもしれない（`idleframe` と同じ扱い）。消したのは4行:
#      trialbg paint 376 842 316 112 pic=home-trial
#      trial   button 376 854 316 112 size=40 tap=trials bind=trials ink=clear
#      partybg paint 376 1476 316 112 pic=home-party
#      party   button 376 1472 316 112 size=40 tap=party ink=clear text=パーティ編成
#    ⭐ これで巣の菱形の真ん中が空き、放置帯と巣だけの画面になった
#    （作者のモックには元からこの3つが無かった）。
