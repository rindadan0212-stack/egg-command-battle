# 探索 ── ⭐ 札は6枚固定（作者の指示 2026-08-29・3→6）
#
# ⚠️ 増やすと「全部見てから決める」になり選択が薄まる、という前提はあったが、
#    作者の指示を優先して6枚にした。
# ⭐ この画面で塗るのは**ヌシの札だけ**（輪の目的地は1つしかない）。
#
# ⚠️ **巻物に入れてある。**実物は 札6枚（380×6）＋ヌシ（380）で下端が
#    下の帯へ大きく潜っていた。⭐ 骨組みでは巻物に入れて、潜らせない。
#
# ⭐ **ヌシは札の後ろに詰める**（`flow=down`）。⚠️ 実物は決め打ちだったので、
#    巣が2件しか出ていないときに札1枚ぶんの穴が空いていた。
#
# ⚠️ **`stack` の高さは 2800**（6×400 + ヌシ400）。`card` の繰り返しの直接の親は
#    `list`（scroll）ではなく `stack`（box）── 巻物なのは `list` だけで `stack` 自身は
#    巻物にならないので、縦のはみ出し検査（`RepeatMaxOverflow`/`OverflowParentY`）は
#    `stack` 自身の高さで見る。低いままだと6枚目やヌシの札が「はみ出し」で
#    `LayoutAssetTests` に落ちる。
#
#  名前    種類    左   上    幅   高    付け足し
# ⭐ **パーティ編成はここ**（2026-08-29・上のバーを外したので右肩から降りてきた）。
#    ⚠️ 前は「本体には置けない ── 巣の札が縦を埋めていて、どこに置いても重なる」と
#    書いてあったが、上のバーが消えて本体が 1556 → 1688 に伸び、巻物の下に 132px 空いた。
# ⭐ **巣を選ぶ前に編成を決める。**⚠️ 潜ってから「違った」と気づいても戻れない
#    ── だからこの画面に置く（`Shell` の `party` が、押した画面で巣／放置を切り分ける）。
party     button  48   1564  984  112   size=34 tap=party text=パーティ編成
list      scroll  48   24    984  1532
  stack   box     0    0     984  2800  flow=down
    card  card    0    0     984  380   repeat=nests cols=1 rows=400 max=6 tap=nest use=encounter
    boss  card    0    20    984  380   lead=yes tap=boss
      # 🔴 **枠は 64 の倍数**（2026-08-29）── ヌシの札は普通の巣の札
      #    （`encounter.txt`）と同じ一覧に並ぶので、同じ 256 に揃える。
      bart  pixel  24  60    256  256   foe=yes bind=bart
      bname label  320 148   600  90    size=56 anchor=upper-left bind=bname
