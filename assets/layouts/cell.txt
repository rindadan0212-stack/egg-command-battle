# 一覧の升1つ ── ⭐ **部品**（器は 224x200）
#
# ⭐ BOX・配合・編成・合成が**同じ升**を使う（読み方が画面ごとに変わらない）。
# ⚠️ 手書きで並べていた頃は、同じ「個体を選ぶ升」が画面ごとに
#    228×200（べた塗りで選択）と 224×200（枠で選択）に割れていた。
#
# ⚠️ **素質の合計を出さない**（2026-08-18・作者判断）。並べ替えれば順に並ぶ。
#    ⭐ 画面ごとの一言（合成の「＋14」・編成の「Lv 44」）だけを下段に出す。
#
#  名前   種類    左   上    幅   高    付け足し
mark     card    0    0     224  200   lead=yes when=picked
# 🔴 **ここは意図的に見切れさせる**（2026-08-29・作者の指示「BOX一覧の升はイラストの
#    一部だけを表示し意図的に見切れさせる。見せたいところだけを見せる」）。
# ⭐ `crop=256` ＝ 絵を 256px で描き、升の窓から覗かせる（顔まわりが大きく見える）。
#    ⚠️ どこを覗かせるかは種族ごとに違う（`SpeciesArt.FocusOf` が持つ）。
# 🔴 **枠いっぱいに広げた**（2026-08-30・作者の指示「枠いっぱいに表示。枠内下に
#    並び替え中の数字か星を表示。イラストと被ってもいい」）。⚠️ 旧 118 角では
#    升（224x200）の中に小さな窓が浮いていて、周りの地が広く空いていた。
# ⚠️ **絵を先に書く。**⭐ あとの節点が上に乗るので、★・属性・一言が絵の上に出る
#    （作者の「イラストと被ってもいい」）── 順を逆にすると絵が全部隠す。
# ⚠️ **`pixel` は正方形でしか描かれない**（`NotSquare` の検査）。⭐ 升は 224x200 なので、
#    入る最大の正方形は 200 角 ── 横を中央へ寄せて置く（左右に 12 ずつ地が残る）。
art      pixel   12   0     200  200   bind=art crop=256
# ⚠️ ここから下は**絵の上に乗る**（作者の「イラストと被ってもいい」）。
#    ⭐ 読めるように、字の下に薄い地（`card ink=faint`）を1枚ずつ敷く ── 絵の色は
#    種族ごとに違うので、地が無いと読める種族と読めない種族が出る。
elem     round   14   14    30   30    bind=elem
starback card    52   0     156  30    ink=faint
star     label   52   2     156  26    size=20 ink=accent anchor=center bind=star
# 🔴 **並び替え中の数字か星を、枠内の下へ**（2026-08-30・作者の指示「枠内下に
#    並び替え中の数字か星を表示」）。⚠️ BOX と配合は一言（`note`）を**出していなかった**
#    ので、何順に並んでいるのかが一覧の上で読めなかった。⭐ 中身は `Sheets` が決める
#    （並べ替えが数で語る基準なら数・入手順なら★）。
# ⚠️ 地は `card`（`ink=faint` の薄い面）── 絵の色は種族ごとに違うので、
#    地が無いと読める種族と読めない種族が出る（実測で確認）。
noteback card    8    150   208  42    ink=faint when=note
note     label   8    154   208  36    size=26 anchor=center bind=note when=note
